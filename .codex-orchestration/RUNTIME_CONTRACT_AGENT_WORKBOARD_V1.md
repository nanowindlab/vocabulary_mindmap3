# RUNTIME_CONTRACT_AGENT_WORKBOARD_V1

## Agent

- Role: `runtime contract lane`
- Owner: `RUNTIME_CONTRACT_AGENT`
- Task ID: `MM3-010`
- Status: `DONE`
- Output Report: `.codex-orchestration/reports/MM3-010_RUNTIME_CONTRACT_PACKAGE_V1.md`

## Scope

- 홈 / 검색·결과 / 상세 / 표현층 / 필터 바에 필요한 runtime contract를 정의한다.

## Required Deliverable

- 화면별 required fields
- `entry`, `sense`, `subword`, `translation`, `filters`가 어느 화면에 어떻게 필요한지
- thin projection과 rich detail의 경계
- runtime payload split 제안
- acceptance에 필요한 required-field 기준
- reflection

## Working Rules

- 한국어 작성
- 구현 지시/코드 변경 금지
- 기본 프로세스 `작업 -> 검증 -> learner 포함 3인 전문가 비판 검토 -> 개선 -> 재검증`

## Result

- 홈 / 결과 / 상세 / 표현층 / 필터 바의 runtime contract package를 한국어로 작성했다.
- 화면별 required fields, `entry` / `sense` / `subword` / `translation` / `filters`의 역할 분담, thin projection과 rich detail의 경계, runtime payload split 제안, acceptance 기준 초안, reflection을 정리했다.
