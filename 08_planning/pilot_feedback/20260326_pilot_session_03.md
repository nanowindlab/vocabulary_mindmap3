# 20260326_pilot_session_03

## Session Meta

- Date: `2026-03-26`
- Session ID: `pilot_session_03`
- Facilitator: `사용자`
- Participant: `사용자`
- Build / URL: `local app / 09_app dev server`
- Scheduled Slot:
  - `ad-hoc follow-up review / 2026-03-26 00:00 KST`
- Raw note copy:
  - `08_planning/pilot_feedback/human pilot test_3차 피드백.md`

## Path

- Session focus terms:
  - `보다`
  - `분류 밖 항목`
  - `Band 범위 legend`
- Completed path:
  - `검색 -> 상세 -> 핵심`
  - `검색 -> 상세 -> 의미 관계`
  - `검색 -> 상세 -> 활용 표현`
  - `검색 -> 상세 -> 예문`
  - `분류 밖 항목 구조 review`
  - `마인드맵 좌측 하단 legend review`

## Normalized Findings

### A. `보다` 상세 surface follow-up

1. `핵심` 탭 top-of-fold helper 밀도는 다시 줄일 필요가 있다.
- note:
  - `상황 미지정` helper와 `표현 활용 워크플로우` card가 뜻 자체보다 먼저 화면을 차지한다.
  - `활용 표현` 탭 count만으로도 discoverability가 충분한지 재검토가 필요하다.
  - 유지한다면 helper card보다 tab count 강조가 더 낫다는 제안이 있었다.

2. `활용 표현` 탭 preview-only copy는 반복이 과하다.
- note:
  - `현재 표제어 안에서 먼저 보는 표현입니다.`
  - `의미 2개 연결`
  - 위 문구가 카드마다 반복되며 정보 밀도 대비 학습 효용이 낮아 보인다는 의견이 있었다.

3. `활용 표현` 탭 번역 visibility는 `번역 ON/OFF`와 같은 규칙을 따라야 한다.
- note:
  - selected language projection 자체는 잘 동작한다.
  - 다만 `번역 OFF`에서도 표현 카드 번역 줄이 계속 보여 `핵심` 탭과 규칙이 어긋난다는 의견이 있었다.

4. `예문` 탭은 helper와 source label 모두 더 간결해질 수 있다.
- note:
  - helper 문구 자체가 꼭 필요한지 다시 확인이 필요하다.
  - `TOPIK 29th`처럼 회차까지 드러내는 표시는 과한 정보로 보인다는 의견이 있었다.
  - source label은 줄 끝 공간이 있으면 같은 줄에서 처리하고, 부족할 때만 줄바꿈하는 식의 압축이 선호됐다.

5. `의미 관계` 탭에는 positive anchor가 있다.
- evidence:
  - `/Users/nanowind/Desktop/스크린샷 2026-03-26 오전 12.00.04.png`
- note:
  - `표현층 / 보기 좋게 / 관용구` 조합의 card hierarchy와 tag 배치가 좋다는 반응이었다.
  - 같은 시각 언어를 `파생어`에도 재사용할 수 있을지 검토 요청이 있었다.

### B. `분류 밖 항목` product / IA

1. fallback surface를 계속 main app 안에 둘지 다시 판단이 필요하다.
- note:
  - learner-facing 쓰임새가 약하면 별도 앱 또는 분리 surface를 검토할 수 있다는 의견이 있었다.

2. main app에 유지한다면 tree order는 `품사 -> 학습난이도`가 더 낫다는 의견이 있었다.
- note:
  - 학습난이도는 이미 filter로도 다룰 수 있어 top-level browse 축으로는 `품사`가 더 직관적이라는 관찰이었다.

### C. Mindmap Chrome

1. 좌측 하단 `Band 범위` legend는 `mm3`에서 다시 value review가 필요하다.
- note:
  - `mm2`에서는 중요했지만, `mm3` current UI에서도 고정 chrome으로 둘 가치가 충분한지 재검토 요청이 있었다.

## Evidence

- Raw note copy:
  - `08_planning/pilot_feedback/human pilot test_3차 피드백.md`
- Screenshot paths:
  - `/Users/nanowind/Desktop/스크린샷 2026-03-25 오후 11.47.35.png`
  - `/Users/nanowind/Desktop/스크린샷 2026-03-26 오전 12.00.04.png`
- Notes:
  - current PM thread의 추가 피드백을 session note로 정규화한 기록이다.
  - relation screenshot의 positive reaction은 keep/reuse anchor로 보관한다.
