# human pilot test_3차 피드백

source:
- current PM thread message
- date: `2026-03-26`

1. `보다`
- `핵심` 탭
  - `/Users/nanowind/Desktop/스크린샷 2026-03-25 오후 11.47.35.png`
  - `상황미지정` 같은 안내문구 의미 있는지? 단어의 의미는 스크롤해서 보아야 하는데.
  - `표현 활용 워크플로우`도 필요한지? `활용 표현`탭에 숫자가 뜨기 때문에 굳이 공간 낭비할 필요 없음.
  - 굳이 강조하고 싶다면 `활용 표현` 탭에 나타나는 숫자에 오렌지색 폰트를 적용?
- `활용 표현` 탭
  - 모든 활용 표현에 `현재 표제어 안에서 먼저보는 표현입니다. 의미 2개 연결` 이란 반복 어구가 있음.
  - 특별한 경우를 표현하기 위함인지? 의미있는지? 없다면 제거.
  - 관용구와 속담에도 영어 표현이 있음.
  - `번역 언어` 필터에 따라 종류가 바뀌어 적용되어 잘 작동.
  - 추가로 `번역ON/OFF` 선택과 무관하게 `활용 표현` 탭의 관용어에서는 관계없이 번역언어가 늘 나타나고 있음.
  - `핵심` 탭에서는 번역 언어가 사라지고 나타나는 것처럼 동일하게 적용할 것.
- `예문` 탭
  - `TOPIK source가 있으면 먼저 보여 주고, 나머지는 현재 의미 예문을 이어서 보여줍니다. source는 구/문장 또는 TOPIK round로 표시하며 최대 8개만 노출합니다.` 같은 안내 사항 필요?
  - 반드시 필요한 의미와 이유가 있지 않다면 제거.
  - `요즘 아이들은 만화도 재미가 있어야 본다니까요. / TOPIK 29th` 에서 TOPIK 회차는 표시할 필요 없음.
  - `TOPIK` 이란 분류가 반드시 줄을 바꿔 표시해야 하는지?
  - `꽃을 보다. / 구` 도 공간이 많이 남지만 줄을 바꾸고 오른쪽 끝에 `구` 한글자만 있음.
  - 같은 줄 끝 부분에 공간이 있다면 활용하고, 없다면 줄바꿈 적용.
- `의미 관계` 탭
  - `/Users/nanowind/Desktop/스크린샷 2026-03-26 오전 12.00.04.png`
  - 첫 구분은 `표현층`, 실제 구문은 `보기 좋게`, 분류는 `관용구`.
  - 이 화면의 UI는 매우 좋다고 생각됨.
  - 같은 방식을 `파생어`에도 적용할 수 있을지 검토.
  - `파생어`라는 구분이 오른쪽 상단에 강조되는 형식이면 좋겠음.

2. `분류 밖 항목`
- 계속 이 단어 사전과 마인드 맵에 연결해서 사용하는 것이 좋을지?
- 아니면 따로 쓰임새가 있으니 분리해서 별도의 앱으로 운영?
- 만약 계속 사용한다면, 현재의 `분류 밖 항목 > 학습난이도 > 품사` 구조에서 `분류 밖 항목 > 품사 > 학습난이도`는 어떨지 검토 요청.
- 실제로 난이도는 필터를 따로 적용할 수 있기 때문에 이 접근이 더 입체적이라고 느낌.

3. 마인드맵 좌측 하단의 `Band 범위` legend 표시
- `mm2`는 이 기능이 중요했지만, `mm3`에서도 따로 표시할 필요가 있을 만큼 가치가 있는지?
